<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.bergerkiller.bukkit</groupId>
  <artifactId>NoLagg</artifactId>
  <version>1.90.0</version>
  <packaging>jar</packaging>

  <name>NoLagg</name>
  <url>http://dev.bukkit.org/server-mods/nolagg/</url>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.libversion>1.5.2-R0.2-SNAPSHOT</project.libversion>
    <project.bkcversion>1.52-SNAPSHOT</project.bkcversion>
  </properties>

  <repositories>
    <!-- Repo for access to Vault -->
    <repository>
      <id>herocraft-repo</id>
      <url>http://ci.herocraftonline.com/plugin/repository/everything/</url>
    </repository>
    <!-- Repo for access to BKCommonLib -->
    <repository>
      <id>dj-tcraft-repo</id>
      <url>http://ci.dj-tcraft.nl/plugin/repository/everything/</url>
    </repository>
    <!-- Repo for access to Libigot -->
    <repository>
      <id>libigot-repo</id>
      <url>http://repo.libigot.org/</url>
    </repository>
  </repositories>

  <dependencies>
    <dependency>
      <groupId>org.libigot</groupId>
      <artifactId>libigot</artifactId>
      <version>${project.libversion}</version>
      <scope>provided</scope>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>com.lishid.orebfuscator</groupId>
      <artifactId>Orebfuscator</artifactId>
      <version>1.8.1</version>
      <scope>system</scope>
      <systemPath>${project.basedir}/lib/Orebfuscator.jar</systemPath>
    </dependency>
    <dependency>
      <groupId>com.bergerkiller.bukkit</groupId>
      <artifactId>BKCommonLib</artifactId>
      <version>${project.bkcversion}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>3.8.1</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <!-- Build -->
  <build>
	<defaultGoal>clean install</defaultGoal>

	<!-- Resources (the filtering allows us to use project properties in plugin.yml) -->
	<resources>
      <resource>
        <directory>src/main/java</directory>
        <filtering>true</filtering>
        <includes>
          <include>plugin.yml</include>
        </includes>
      </resource>
	</resources>

	<!-- Plugins -->
	<plugins>
      <!-- Compiling the source code -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>2.3.2</version>
        <configuration>
          <source>1.6</source>
          <target>1.6</target>
		  <showWarnings>true</showWarnings>
		  <showDeprecation>true</showDeprecation>
        </configuration>
      </plugin>

      <!-- Manifest generation for examiner GUI -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-jar-plugin</artifactId>
        <version>2.4</version>
        <configuration>
          <archive>
            <manifestEntries>
              <Main-Class>com.bergerkiller.bukkit.nolagg.examine.reader.ExamReader</Main-Class>
              <Implementation-Title>${project.name}</Implementation-Title>
              <Implementation-Version>${project.version}</Implementation-Version>
            </manifestEntries>
          </archive>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>